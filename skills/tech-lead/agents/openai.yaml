interface:
  display_name: "Tech Lead"
  short_description: "Create a delivery plan whose scope, dependencies, risks,..."
  default_prompt: "Use $tech-lead to complete this task with evidence."
policy:
  allow_implicit_invocation: true
